/**
 * Representa una respuesta simple de la API que contiene solo un mensaje.
 *
 * @property message - Mensaje informativo o de confirmación.
 */
export type MessageResponseDTO = {
  /** Mensaje de la respuesta */
  message: string;
};
